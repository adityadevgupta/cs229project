from music21 import *
from util import *
import os

class Trainer:

	def __init__():
		self.dir = '../midi/'
		
	def get_training_set():
		# go through every midi file in the directory
		train_X = []
		for file in os.listdir(self.dir):
			
			# get the raw training point from the ScoreParser
			# convert the raw training point into an integer representation

	def train():
		return None